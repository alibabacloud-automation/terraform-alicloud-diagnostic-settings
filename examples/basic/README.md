# Basic Example

This example demonstrates the basic usage of the Alibaba Cloud Diagnostic Settings module.

## Usage

To run this example, execute the following commands:

```bash
terraform init
terraform plan
terraform apply
```

Make sure to configure your Alibaba Cloud credentials before running the example.